<UserControl x:Class="UtkarshShigihalli.ProgressWindowDemo.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="900"
             Name="MyToolWindow">
    <Grid>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="10">
            <Button Content="_Normal Waiting Dialog"
                    Click="ThreadedNonCancellable_Click"
                    Height="38"
                    Margin="5"
                    Padding="5" />
            <Button Content="With _Cancel button"
                    Click="OnShowWithCancelButton"
                    Height="38"
                    Margin="5"
                    Padding="5" />
        </StackPanel>
    </Grid>
</UserControl>
